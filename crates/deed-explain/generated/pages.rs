// @generated by `cargo test -p deed-driver --test explain_pages -- --ignored`.
// The reasoning comes from the doc comments above each `pub const` in a
// `codes.rs`; the example comes from a test that already had to exist.
// Edit those, not this.

pub static PAGES: &[Page] = &[
    Page {
        code: "DEED7001",
        name: "UNKNOWN_DIRECTIVE",
        text: "A line in the manifest that is not blank, not a comment, and not a\nrecognized directive.\n\nThe only directive today is `component <path>`. Anything else on a\nnon-blank, non-comment line is this error.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED7002",
        name: "MISSING_COMPONENT_PATH",
        text: "A `component` directive with no path following it.\n\n`component` on a line by itself, or followed only by whitespace, does not\nsay where the component lives and cannot be acted on.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED7003",
        name: "MISSING_MODULE_SOURCE",
        text: "A `module` directive with nothing after it.\n\nA location says where the bytes were and a hash says what they are. With\nneither there is nothing to fetch and nothing to check it against.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED7004",
        name: "MISSING_MODULE_HASH",
        text: "A `module` directive with a location and no hash.\n\nSeparate from [`MISSING_MODULE_SOURCE`] because it is a different mistake\nwith a different answer: the reader knows where the module is and has to be\ntold that knowing is not enough. A dependency without a hash is one whose\nbytes are whatever the other end felt like today, and a build that accepts\nthose cannot be repeated.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED7005",
        name: "BAD_MODULE_HASH",
        text: "A hash that is not sixty-four lowercase hexadecimal digits after `sha256:`.\n\nOne code for every way of writing it wrong, because they are all the same\nmistake seen from different angles and each one names what it found.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED7006",
        name: "MODULE_NOT_FETCHED",
        text: "Bytes that could not be retrieved, or that did not hash to what the\nmanifest said they would.\n\nA mismatch is never a warning and never a retry. The cache is keyed by the\nexpected hash, so bytes that do not match are bytes for a different\ndependency, and storing them would answer a later build with them.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED5001",
        name: "UNDECLARED_EFFECT",
        text: "A body performs an effect its signature does not declare.",
        example: Some("module a\n\nfn quiet(out: Console) -> () {\n  Io.write(out, \"hello\")\n}\n"),
        example_source: Some("crates/deed-driver/tests/capabilities.rs"),
    },
    Page {
        code: "DEED5002",
        name: "UNUSED_EFFECT",
        text: "A signature declares an effect its body never performs.\n\nThis is the rule that decides whether any of this is worth having. If\nover-declaring were allowed, every signature would drift towards listing\neverything, and a row nobody can trust is a row nobody reads.",
        example: Some("module a\n\nfn total(numbers: List<Int>) -> Int\n  uses\n    Diverge,\n{\n  for n in numbers with sum = 0 {\n    sum + n\n  }\n}\n"),
        example_source: Some("crates/deed-driver/tests/iteration.rs"),
    },
    Page {
        code: "DEED5003",
        name: "NOT_AN_EFFECT",
        text: "A `uses` entry naming something that is not an effect.\n\nAn imported name lands here too. Which kind of thing it is comes off the\nexport rather than off a declaration in this file, but it comes off\nsomething either way, so the module boundary does not soften the answer.",
        example: Some("module a\n\nfn apply(f: Fn(Int) uses Int -> Int, n: Int) -> Int { f(n) }\n"),
        example_source: Some("crates/deed-driver/tests/function_rows.rs"),
    },
    Page {
        code: "DEED5004",
        name: "UNVERIFIABLE_ROW",
        text: "A row the compiler cannot check.\n\nTwo shapes are written in a `uses` clause and reported where they are\nwritten: `sys.*`, which grants everything a capability carries, and a bare\n`sys`, which names a value where an effect belongs. Both leave a row that\nmeans nothing, and the compiler says so rather than reporting a clean check\nit did not perform.\n\nThe third is reported at a call into another module, and it does not follow\nfrom either of those. It follows from the star and from nothing else. An\nexport carries `row_complete`, and `RowLowering::row` sets it from the\nsyntax of the clause: false as soon as any entry is starred, whatever the\nstar is on, with the starred entry left out of the exported row rather than\ncarried. So `uses Log.*` on a declared effect is reported at every call site\nwhile the declaration itself is silent, because locally it means the whole\nof `Log` and checks fine, and `uses sys,` is reported where it is written\nand at no call site, because it is not starred: it crosses as an ordinary\nentry and the caller is told by DEED5006 to import a name that is a\nparameter.\n\nNeither of those is the answer anyone would choose. They hold because a\nmodule's surface is lowered from syntax alone, on purpose, so that it can be\ncomputed before anything is resolved, and whether a name is an effect or a\ncapability is exactly what the syntax does not say. Deciding it properly\nmeans resolving before exporting, which changes how rows cross the boundary\nrather than what this code says about them. So it is written down here and\nin design/03-effects.md instead of being changed here.\n\nA `uses` entry that is simply the wrong kind of name is not here. That is\nDEED5003, whether the name is local or imported: the compiler knows what it\nis, so there is nothing it failed to verify.",
        example: Some("module wide\n\nfn everything(sys: System) -> Int\n  uses sys.*,\n{ 0 }\n"),
        example_source: Some("crates/deed-effects/tests/effects.rs"),
    },
    Page {
        code: "DEED5005",
        name: "UNHANDLED_EFFECT",
        text: "A test performs an effect with no handler in scope.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED5006",
        name: "EFFECT_NOT_IMPORTED",
        text: "An effect that arrived through an imported call and cannot be named here.\n\nA function cannot promise something it has no word for. Calling something\nthat logs means importing `Log`, which follows from the row being the review\nsurface: a row that could not name what it grants would not be one.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED5007",
        name: "IMPURE_FUNCTION_VALUE",
        text: "A function value that performs more than the type it crosses into allows.\n\n`Fn(Int) -> Int` promises no effects and\n`Fn(Int) uses Log.note -> Int` promises no more than that. Leaving a row\noff cannot mean \"any row\": a value that carried an unstated effect through\na signature would undo the point of having rows at all.",
        example: Some("module a\n\neffect Log {\n  fn note(message: String) -> ()\n}\n\nfn apply(f: Fn(Int) -> Int, n: Int) -> Int { f(n) }\n\nfn go(n: Int) -> Int\n  uses Log.note,\n{\n  apply(|x: Int| { Log.note(\"hi\") x }, n)\n}\n"),
        example_source: Some("crates/deed-effects/tests/messages.rs"),
    },
    Page {
        code: "DEED5008",
        name: "MISPLACED_ROW_VARIABLE",
        text: "A row variable written somewhere a call site could not fill it in.\n\nA row variable stands for whatever a callback performs, and the only thing\nthat knows what that was is the call that supplied the callback. So it has\nto be readable off an argument: the row of a parameter whose type is a\nfunction type, and the declaration's own `uses` clause. Written anywhere\nelse, such as a return type, it leaves the call site holding a value whose\nrow names something no caller has a word for, and an effect nobody can name\nis an effect nobody declares.\n\nThe same shape as DEED4023, where a type parameter has to appear in a\nparameter's type so that every call knows what it is.",
        example: Some("module p\n\neffect Task<uses r> {\n    fn next() -> Fn() uses r -> ()\n}\n"),
        example_source: Some("crates/deed-driver/tests/row_parameters.rs"),
    },
    Page {
        code: "DEED5009",
        name: "CONTRACT_EFFECT_NOT_DECLARED",
        text: "A contract performs an effect the signature does not mention.\n\nA contract does not contribute to a row, which is why the row may be\nnarrower than the clauses: `examples/transfer.deed` reads `Ledger.total()`\nin an `ensures` clause and never declares it. What the row cannot be is\nsilent about the effect altogether. Installing a handler is the caller's\njob, and the signature is the only place a caller can find out that one is\nneeded, so a clause performing an effect nothing above the brace names is a\ncall that type checks and then cannot run.\n\nThe granularity is the effect rather than the operation, because that is\nwhat a handler is installed for, and because asking for the operation would\nmean asking for a row entry that DEED5002 then rejects as unused.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6001",
        name: "ASSERTION_FAILED",
        text: "An `assert` that was not true, or an `assert refuses` that nothing refused.\n\nTwo messages. The second is the half that makes the first worth having: a\nstatement that passed whatever happened would be the same mistake as a test\nthat only feeds a guard values it accepts.",
        example: Some("module a\n\nfn double(n: Int) -> Int { n + n }\n\ntest \"wrong\" {\n  assert double(3) == 7\n}\n"),
        example_source: Some("crates/deed-cli/tests/cli.rs"),
    },
    Page {
        code: "DEED6002",
        name: "PRECONDITION_FAILED",
        text: "A `where` clause that did not hold on entry. A bug in the caller.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6003",
        name: "POSTCONDITION_FAILED",
        text: "An `ensures` obligation that did not hold on exit. A bug in the function.",
        example: Some("module a\n\nfn wrong(n: Int) -> Int\n  ensures ok => result == n + n,\n{ n + 1 }\n\ntest \"catches it\" {\n  assert wrong(5) == 6\n}\n"),
        example_source: Some("crates/deed-driver/tests/result_binding.rs"),
    },
    Page {
        code: "DEED6004",
        name: "REFINEMENT_FAILED",
        text: "A value that did not satisfy the refinement it was passed into.\n\nThis is the `Guarded` tier actually guarding something.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6005",
        name: "NO_HANDLER",
        text: "An effect that could not be performed by anything installed.\n\nFour messages. Two are about the program and they are opposite readings of\nthe same situation, which is why they are separate sentences. An effect\nthat reached `main` is in the row a component's world is derived from, so\nnothing inside the program was ever going to answer it and a host is meant\nto; an effect loose anywhere else is a missing `with` block. The other two\nare about the compiler: a handler that leaves an operation out is refused\nby `DEED4029`, and an `Io` operation handed the wrong kind of capability is\na type error, so a run that meets either has been given a file the checker\nwould not have accepted.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6006",
        name: "NOT_RUNNABLE",
        text: "Something the run could not do.\n\nThirty-six messages, which is three quarters of everything in this range,\nand they are not one thing. Thirty-two arrive through one helper and say\nonly that the run met a shape `deed check` refuses first, so meeting one\nmeans the file was not checked or the check has a hole; one is a call into\na module whose code was never handed over, which is a gap in what this\nlibrary was given rather than in the library; one is `sys.files` in a\nprogram that was not given a directory, which is an ordinary runtime fact\nabout a program that is right; and two name a particular part of the\ncompiler, a match that ran out of arms and a handler with a state field\nnobody gave a value to.\n\nFour notes say which of those a reader has. There used to be one, claiming\nall thirty-six were the interpreter's own unfinished work.\n\nTwo of them said the opposite, and were the only two that could: handler\nstate read from a closure that outlived the handler operation it was\nwritten in, which `deed check` accepted. It does not any more. `DEED4030`\nrefuses the closure where it is written, so those two joined the thirty\nand the fifth note went away with the helper behind it.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6007",
        name: "ARITHMETIC",
        text: "Arithmetic that has no answer, such as overflow or division by zero.",
        example: Some("module a\n\ntype Positive = Int where value > 0\n\nfn grow(n: Positive) -> Positive { n + 1 }\n\ntest \"the boundary\" {\n  assert grow(9223372036854775807) == 0\n}\n"),
        example_source: Some("crates/deed-driver/tests/agreement.rs"),
    },
    Page {
        code: "DEED6008",
        name: "NOT_ENOUGH_CASES",
        text: "A generated property test that could not find enough usable inputs.\n\nA property that only ever tested a handful of inputs is worse than no\nproperty, because it looks like one.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6009",
        name: "TOO_DEEP",
        text: "A call that went deeper than the interpreter is willing to go.\n\n`Diverge` in a row says a function may not return. It does not make one\nreturn, so the runtime needs an answer for the case where it does not, and\nthe answer is a diagnostic rather than the process dying.",
        example: Some("module a\n\nfn forever(n: Int) -> Int\n  uses\n    Diverge,\n{\n  forever(n + 1)\n}\n\ntest \"never\" {\n  assert forever(0) == 0\n}\n"),
        example_source: Some("crates/deed-interp/tests/running.rs"),
    },
    Page {
        code: "DEED6010",
        name: "ROW_NOT_KEPT",
        text: "An effect was performed inside a function that did not declare it.\n\nNot a mistake in the program. The file was accepted, so this says the\neffect checker let something through, and the run is the thing that noticed.\nThe rows are the argument this language is making, and until this existed\nthe only thing that ever read one was the pass that wrote it.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED6011",
        name: "ABANDONED",
        text: "A computation was abandoned by its handler.\n\nA handler operation executed `abandon` instead of returning a value. The\ncomputation that performed the effect does not receive a return value;\ninstead the stack unwinds, running every `finally` clause it passes\nthrough, and then the run stops with this code.\n\nThis is not a contract failure and `assert refuses` cannot catch it.",
        example: Some("module a\n\neffect Gate {\n    fn enter() -> Int\n}\n\nhandler Closed implements Gate {\n    fn enter() -> Int {\n        abandon\n    }\n}\n\nfn ask() -> Int uses Gate.enter { Gate.enter() }\n\ntest \"abandon raises DEED6011\" {\n    with Closed {\n        ask()\n    }\n}\n"),
        example_source: Some("crates/deed-driver/tests/dispatch.rs"),
    },
    Page {
        code: "DEED1001",
        name: "UNKNOWN_CHARACTER",
        text: "A character that cannot begin any token.",
        example: Some("\\u{FEFF}module a\n"),
        example_source: Some("crates/deed-lexer/tests/lexing.rs"),
    },
    Page {
        code: "DEED1002",
        name: "UNTERMINATED_STRING",
        text: "A string literal that reached a newline or end of file before its closing quote.",
        example: Some("let a = \"oops\nlet b = 1"),
        example_source: Some("crates/deed-lexer/tests/lexing.rs"),
    },
    Page {
        code: "DEED1003",
        name: "UNTERMINATED_BLOCK_COMMENT",
        text: "A block comment that reached end of file before its closing delimiter.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED1004",
        name: "UNKNOWN_ESCAPE",
        text: "An escape sequence the language does not define.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED1005",
        name: "INTEGER_OUT_OF_RANGE",
        text: "An integer literal that does not fit in `Int`.",
        example: Some("module a\n\nfn f() -> Int {\n  9223372036854775808\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED1006",
        name: "MALFORMED_NUMBER",
        text: "A numeric literal with no digits, a digit invalid for its radix, or a suffix.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED1007",
        name: "NO_FLOAT_LITERAL",
        text: "A decimal point between two digits, which the language has no literal for.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2001",
        name: "UNEXPECTED_TOKEN",
        text: "A token that cannot appear where it was found.",
        example: Some("module a\n\nfn broken(x: ) -> Int { 0 }\n\nfn g() -> Int { balanse() }\n"),
        example_source: Some("crates/deed-cli/tests/cli.rs"),
    },
    Page {
        code: "DEED2002",
        name: "MISSING_MODULE_DECLARATION",
        text: "A file that does not begin with a `module` declaration.",
        example: Some("fn f() -> Int { 0 }\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2003",
        name: "EXPECTED_DECLARATION",
        text: "A token at the top level that cannot begin a declaration.",
        example: Some("module a\n\n42\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2004",
        name: "DUPLICATE_CONTRACT_CLAUSE",
        text: "A contract clause given twice on one function.",
        example: Some("module a\n\nfn f()\n  where true,\n  where false\n{ 0 }\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2005",
        name: "INVALID_ENSURES_OUTCOME",
        text: "An `ensures` obligation whose outcome is neither `ok` nor `err`.\n\nAlso the clause that names no outcome at all, where the condition itself\nstands where `ok =>` belongs. It is the same gap seen from the other side,\nand it used to be reported twice: once for the word in the outcome's place\nand once for the `=>` that never came. Now the parser reads the rest as the\ncondition it plainly is, and there is one sentence and one repair.",
        example: Some("module a\n\nfn f() -> Int\n  ensures nope => true\n{ 0 }\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2006",
        name: "CONTRACT_CLAUSE_ORDER",
        text: "Contract clauses written in an order other than `where`, `uses`, `ensures`.\n\nP4 says there is one canonical form. Clause order is part of it: a signature\nis the review surface, and it should read the same way every time.",
        example: Some("module a\n\nfn f()\n  uses E,\n  where true\n{ 0 }\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2007",
        name: "MISSING_PARAMETER_TYPE",
        text: "A function parameter written without a type.\n\nP5 says nothing implicit crosses a boundary, and a parameter is the\nboundary. An untyped one used to become the unknown type, which agrees with\neverything, so every mistake made with it was invisible and a closure could\ncarry any effect through it into a function that declared none.",
        example: Some("module a\n\nfn f(n) -> Int { n }\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2008",
        name: "POSITIONAL_VARIANT",
        text: "A choice variant given its payload by position rather than by name.\n\n`Circle(Int)` is what anyone arriving from a language with tuple variants\nwrites first, and it is refused. Saying so as \"expected `}`\" reads as a\ntypo in a line that has none, so it has a code of its own.\n\nWhether it should be refused at all is open. `ok` and `err` carry a value\npositionally and are built in, which is the shortcut `design/02-syntax.md`\nrecords under what holds `Result` in the language.",
        example: Some("module a\n\nchoice Shape {\n  Circle(Int),\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2009",
        name: "NO_BINDING_MODIFIER",
        text: "A word in front of a `let` name, such as `mut`, that the language has no\nplace for.\n\nIt has a code of its own because taking `mut` as the name is the reading\nthat produces the most and the least useful messages of any single word a\nnewcomer writes, and none of them names the word.",
        example: Some("module a\n\nfn f() -> Int {\n  let mut n = 1\n  n\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2010",
        name: "BINDING_WITHOUT_LET",
        text: "A binding written without `let`, either behind another language's keyword\n(`var n = 1`) or behind its type (`Int n = 1`).\n\nBoth used to arrive as a name nobody declared and an assignment to a name\nnobody declared, which is two messages about the halves and none about the\nline. The shape is safe to read because two names in a row on one line is\nnot a statement here.",
        example: Some("module a\n\nfn f() -> Int {\n  var n = 1\n  n\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2011",
        name: "NO_RANGE",
        text: "`0..10`. A range, which this language does not have.\n\nThe two dots used to be left where they were, and the rest of the file paid\nfor it: `for i in 0..10` produced six diagnostics and the next declaration\nwas reported as not being one. Two dots in a row are never anything else,\nsince a field access has a name between them, so the shape can be read and\nswallowed whole.",
        example: Some("module a\n\nfn f() -> () {\n  for i in 0..10 {\n    i\n  }\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2012",
        name: "NO_CAST",
        text: "`n as String`. A cast, which this language does not have.\n\n`as` is an ordinary name, so the resolver used to answer it with \"cannot\nfind `as` in this scope\", which is true of a word nobody wrote as a name.\nThe conversions that exist are calls, and a call says in its return type\nwhether it can fail, which is the thing a cast is for hiding.",
        example: Some("module a\n\nfn f(n: Int) -> String {\n  n as String\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED2013",
        name: "UNKNOWN_EDITION",
        text: "An edition declaration the parser does not recognize.\n\nEditions are accepted per module, and an unknown one should fail at the\ndeclaration line with a list of the versions that exist.",
        example: Some("module a edition 2099\n\nfn f() -> Int { 0 }\n"),
        example_source: Some("crates/deed-parser/tests/parsing.rs"),
    },
    Page {
        code: "DEED2014",
        name: "NO_DETACHED_SPAWN",
        text: "`spawn(f())`. A detached spawn, which this language does not have.\n\nDeed uses structured concurrency: a task is tied to the block that started\nit and cannot outlive it. A detached spawn leaves a task running after the\nblock that created it exits, with no clear owner and no scoped lifetime.\nThat shape is refused rather than allowed to leak.\n\nThe pattern is detected at `spawn(expr)` where `spawn` is an identifier at\nstatement level followed by an argument list on the same line.",
        example: Some("module threshold/spawn\n\nfn work() -> Int {\n    1\n}\n\nfn start() -> () {\n    spawn(work())\n}\n\ntest \"unreachable\" {\n    assert start() == ()\n}\n"),
        example_source: Some("crates/deed-driver/tests/thresholds.rs"),
    },
    Page {
        code: "DEED2015",
        name: "MISSING_COMMA",
        text: "A comma-separated list written one to a line with no commas.\n\nA separator nobody wrote is a whole file's worth of complaints. In a match\nthe first arm swallows the rest of it, the arms after are read as\nstatements, and what comes back is an inexhaustive match, an unread value\nper arm, an expected `}`, an expected expression at each `=>` and a\ndeclaration that is a closing brace. Nine diagnostics for one comma, and\nnone of them said comma. A `choice` written the same way says \"insert `}`\",\nwhich is an answer to a question nobody asked.\n\nReported where the comma should have gone rather than where the parser\nnoticed, and it goes on reading the list afterwards, so three missing\ncommas is three of these rather than one and then rubble.\n\nThree sentences under one code, one per list: arms, variants and fields.\nThey are the same mistake and the same repair, and the word that changes is\nthe name of the thing being separated.",
        example: Some("module a\n\nchoice Grade {\n    Low,\n    Mid,\n    High,\n}\n\nfn describe(mark: Grade) -> String {\n    match mark {\n        Low => \"low\"\n        Mid => \"mid\"\n        High => \"high\"\n    }\n}\n"),
        example_source: Some("crates/deed-parser/tests/parsing.rs"),
    },
    Page {
        code: "DEED2016",
        name: "WRONG_ARROW",
        text: "A match arm written with `->` instead of `=>`.\n\nBoth arrows are in the language and they are a line apart: `->` is the one\nin a signature, before the type a function hands back, and `=>` is the one\nin an arm and in an obligation. Reaching for the wrong one is a slip of the\nhand rather than a misunderstanding, and it used to cost four diagnostics,\nbecause the arm ended at the pattern and the body after it was read as a\nstatement of the match's block.\n\nSo it is named, repaired and stepped over, and the arm goes on being read\nas an arm.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2017",
        name: "PARAMETER_CONSTRAINT",
        text: "A constraint written on a parameter rather than in the function's `where`.\n\nA `type` carries its refinement inline (`type InStock = Int where value >\n0`), so writing the same thing on a parameter is a fair guess rather than a\nmisunderstanding. A function carries its constraints in the clause after\nthe signature instead, which is the only place every parameter is in scope\nat once, and `restock(count: Int where count + delivered > 0, ...)` is the\ncase that makes the difference obvious.\n\nNamed, and then put where it belongs: the expression is read into the\nfunction's `where` clause, so the names in it resolve and the four\ndiagnostics this used to cost come down to this one.",
        example: Some("module a\n\nfn f(n: Int where n > 0, m: Int) -> Int\n  where m > 0,\n{ n + m }\n"),
        example_source: Some("crates/deed-parser/tests/parsing.rs"),
    },
    Page {
        code: "DEED2018",
        name: "NO_LIST_OPERATOR",
        text: "`xs ++ ys` or `x :: xs`: an operator borrowed from a language that has one.\n\nBoth are doubled, and nothing in this grammar puts two `+` or two `:` in a\nrow, so the shape can be read where it is written rather than left to fall\napart. Left alone, `++` costs an expected expression and `::` costs an\nunread value and two more, none of which mention a list.\n\nTwo sentences under one code, because it is one mistake: this language\nbuilds lists by calling something. The sentence that changes names the\ncall, `concat` for one and `prepend` for the other, and says the list goes\nfirst, which is the part that is easy to get backwards.",
        example: Some("module a\n\nfn joined(l: List<String>, r: List<String>) -> List<String> {\n    l ++ r\n}\n"),
        example_source: Some("crates/deed-parser/tests/parsing.rs"),
    },
    Page {
        code: "DEED2019",
        name: "STATE_INITIALISER",
        text: "`state count: Int = 0`: a handler's state given its value in the handler.\n\nThe value comes from the `with` that installs the handler, which is what\nlets one handler be installed twice from two different starting points.\nWritten on the declaration, it used to end the handler: the state stopped\nat the type, the `=` was not a member, and what came back was seven\ndiagnostics, one of them saying the handler implements none of its\noperations and four about names in the operations the parser never reached.\n\nRead and dropped, so the operations after it are still operations, and the\nnote says both halves of the shape rather than which token was wanted.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2020",
        name: "WORD_OPERATOR",
        text: "`a and b`, `a or b`: the words other languages spell their operators with.\n\nThe resolver already had an answer for these, and it never got to give it.\n`and` is an ordinary name here, so a condition or a contract clause holding\none stops at the word: the expression ends, and the reader is told a block\nwas expected. Nothing in that sentence is the word they wrote.\n\nRead as the operator they mean, so the line is the program they wrote and\nthe rest of it is checked. The repair is a word for a symbol, which is the\nwhole of it, so it is applied rather than offered.",
        example: Some("module a\n\nfn twice(n: Int) -> Int\n  where n >= 0 and n <= 10\n  ensures ok => result == n + n\n{\n    n + n\n}\n"),
        example_source: Some("crates/deed-parser/tests/parsing.rs"),
    },
    Page {
        code: "DEED2021",
        name: "BRACELESS_BODY",
        text: "A signature with the answer written under it and no braces around it.\n\nThe shape other languages give a one-line function. Here a body is a block\nand nothing else, so the reader was told a `{` was expected, the body was\nskipped, and its first line was then read as a declaration that was not\none. What arrived was a message about a brace, a message about a name, and\na return type that no longer matched anything.\n\nRead to the next declaration, which is the only thing that can follow a\nbody, so the braces are an insertion at each end and the function is the\none that was written.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2022",
        name: "RETURN_TYPE_AFTER_CONTRACT",
        text: "`fn f(n: Int) where n > 0 -> Int`: the contract written before the type.\n\nThe order here is the parameter list, then `->` and the type, then the\ncontract, because `->` is what closes a parameter list and what follows the\ntype is about what the function promises rather than about its shape.\n\nWritten the other way the arrow sat where the body should have begun, so\nthe reader was told a `{` was expected, and the function went on to have no\nreturn type and a body that did not match the one it was missing.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2023",
        name: "OUTCOME_IN_WHERE",
        text: "`where ok => n + n <= 10`: an outcome written on a precondition.\n\nWhat somebody writes after reading the `ensures` beside it. A `where`\nclause is about the arguments, which are what they are whichever way the\ncall comes out, so there is no outcome for one to name.\n\nIt used to end the contract at the `=>`, so the reader was told a block was\nexpected, and told separately that `ok` is a builtin rather than a value.\nNeither of those says which clause an outcome belongs to.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2024",
        name: "EFFECT_TYPE_PARAM",
        text: "`effect Queue<T>`: a type parameter on an effect declaration.\n\nAn effect's parameter list holds row variables and nothing else. A row\nvariable is filled in at the calls that supply it and erased everywhere\nelse, so one costs nothing to carry. A type parameter would have to reach\nthe handler, which is a value with state and a single installation, and\nthere is no answer yet for what `with Q { items: [] }` means when two\ndifferent `T`s are performed through it.\n\nWritten as its own message because the list does parse, and the reader\notherwise gets a name-resolution error about `T` from inside an operation\nsignature, which points at the use rather than the decision.",
        example: Some("module p\n\neffect Queue<T> {\n    fn add(item: T) -> ()\n}\n"),
        example_source: Some("crates/deed-driver/tests/row_parameters.rs"),
    },
    Page {
        code: "DEED2025",
        name: "UNBINDABLE_OPERATOR",
        text: "`operator / = divided`: an operator a module may not give a meaning to.\n\nA module can bind `+`, `-` and `*` to one of its own functions, and the\nthree were chosen rather than left over. An operator answers with the type\nit was handed, and `/` and `%` do not always have such an answer, which is\nwhat a `Result` is for and what a function returning one already says.\nEquality is structural and total over every type, so there is nothing for a\nmodule to decide. The comparisons are a separate decision, because ordering\na user type runs into generic sorting rather than stopping at notation.\n\nThe binding parses either way, so the reader is told which operator is the\nproblem rather than being shown the whole declaration form. See\n`design/decisions/2026-08-03-operators-bound-to-functions.md`.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2026",
        name: "EMPTY_INTERFACE",
        text: "`effect Random from \"\"`: an interface name with nothing in it.\n\nThe name is what a compiled component's world asks its host for, so an\nempty one produces an import nothing can satisfy and a world nobody can\nread. Leaving the clause off is the way to say \"this effect is its own\ninterface\", and it is a different thing from writing one and leaving it\nblank, which is why this is refused rather than treated as absent.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED2027",
        name: "FIELD_ASSIGNMENT",
        text: "`point.x = 1`, an assignment to a field.\n\n`name = value` is an assignment this language has, because a handler's\nstate is the one thing that can be written to. The field form is not, and\nit used to arrive as two messages about neither: the receiver was read as\nan expression statement and the `=` after it as the start of the next one,\nso the reader got \"expected an expression, found `=`\" about a line whose\nreal problem was the shape in front of it.\n\nThe measured case is `state.entries = state.entries + [entry]`, written by\nsomebody reaching for the handler state through a receiver the way another\nlanguage would. That one gets a second sentence: the state is named on its\nown, and the handler it belongs to is the one being run.\n\nNo repair. Changing one field of a record is another record literal, which\nis a rewrite of the line rather than an edit to it, and taking `state.` off\nwould be wrong for anybody who bound a record to that name.",
        example: Some("module a\n\nrecord Point { x: Int }\n\nfn f(point: Point) -> Int {\n    point.x = 1\n    point.x\n}\n"),
        example_source: Some("crates/deed-parser/tests/messages.rs"),
    },
    Page {
        code: "DEED3001",
        name: "UNKNOWN_NAME",
        text: "A name that does not refer to anything in scope.",
        example: Some("module a\n\nfn broken(x: ) -> Int { 0 }\n\nfn g() -> Int { balanse() }\n"),
        example_source: Some("crates/deed-cli/tests/cli.rs"),
    },
    Page {
        code: "DEED3002",
        name: "DUPLICATE_DEFINITION",
        text: "Two declarations of the same name in one module.",
        example: Some("module a\n\nuse a.{f}\n\nfn f() -> Int { 0 }\n"),
        example_source: Some("crates/deed-driver/tests/modules.rs"),
    },
    Page {
        code: "DEED3003",
        name: "UNUSED_IMPORT",
        text: "An imported name that is never used.",
        example: Some("module two\n\nuse std/list.{nonesuch}\n\nfn f() -> Int {\n    1\n}\n"),
        example_source: Some("crates/deed-cli/tests/agreement.rs"),
    },
    Page {
        code: "DEED3004",
        name: "SHADOWED_BINDING",
        text: "A binding that hides another binding.\n\nThis is an error rather than a warning, and that is a real decision. See\n`design/02-syntax.md`.",
        example: Some("fn handled(limit: Int) -> Int {\n  match small(limit) {\n    ok(value) => value,\n    err(TooBig { limit }) => limit,\n    err(Empty) => 0,\n  }\n}\n"),
        example_source: Some("crates/deed-driver/tests/result_patterns.rs"),
    },
    Page {
        code: "DEED3005",
        name: "SHADOWED_DECLARATION",
        text: "A binding that hides a declaration from the module or the prelude.",
        example: Some("module a\n\nfn f<Int>(value: Int) -> Int { value }\n"),
        example_source: Some("crates/deed-driver/tests/generics.rs"),
    },
    Page {
        code: "DEED3006",
        name: "UNKNOWN_MEMBER",
        text: "A qualified name whose container has no such member.",
        example: Some("module a\n\neffect Ledger {\n  fn balance(id: Int) -> Int\n}\n\nfn f() -> Int\n  uses Ledger.teleport,\n{ 0 }\n"),
        example_source: Some("crates/deed-resolve/tests/resolving.rs"),
    },
    Page {
        code: "DEED3007",
        name: "UNKNOWN_MODULE",
        text: "A `use` of a module that is not among the files being compiled.",
        example: Some("module two\n\nuse std/list.{nonesuch}\n\nfn f() -> Int {\n    1\n}\n"),
        example_source: Some("crates/deed-cli/tests/agreement.rs"),
    },
    Page {
        code: "DEED3008",
        name: "UNKNOWN_EXPORT",
        text: "A `use` of a name the module does not declare.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED3009",
        name: "UNUSED_BINDING",
        text: "A binding nobody reads.\n\nA `let` binding, a pattern binder, a `for` binder or a parameter that no\nexpression mentions. Shadowing is not allowed, so a name is bound once and\nmeans one thing for the whole function, which is what makes this decidable\nby looking rather than by tracking. A leading underscore is how a program\nsays it meant to ignore something.",
        example: Some("module a\n\nfn f() -> Int {\n  let spare = 1\n  2\n}\n"),
        example_source: Some("crates/deed-resolve/tests/resolving.rs"),
    },
    Page {
        code: "DEED3010",
        name: "BINDING_IN_AN_ALTERNATIVE",
        text: "An alternative in a match arm that would bind a name.\n\n`Plus | Times` names two variants so that one body can serve both, and it\nis cheap precisely because neither side binds. A language whose\nalternatives can bind has to require that every one of them binds the same\nnames, so that the body finds what it reads whichever side matched, and\nthat rule is the entire cost of the feature. Refusing to bind at all is the\nversion with none of it.\n\nNothing is lost. A variant with fields can be matched by name alone, which\nis what every arm that wanted this was already doing with the fields it\nnever read.",
        example: Some("module a\n\nchoice E { A { n: Int }, B }\n\nfn f(e: E) -> Int {\n  match e {\n    A { n } | B => n,\n  }\n}\n"),
        example_source: Some("crates/deed-resolve/tests/resolving.rs"),
    },
    Page {
        code: "DEED3011",
        name: "DEPRECATED_DECLARATION",
        text: "A reference to a declaration marked `deprecated`.\n\nThe declaration still exists for now, so this is a warning, but the warning\ncarries the replacement and may carry a machine-applicable fix.",
        example: Some("module a\n\ndeprecated legacy -> replacement\nfn legacy() -> Int { 1 }\nfn replacement() -> Int { 2 }\nfn f() -> Int { legacy() }\n"),
        example_source: Some("crates/deed-resolve/tests/resolving.rs"),
    },
    Page {
        code: "DEED3012",
        name: "AMBIGUOUS_MODULE",
        text: "Two source files in the same compile both declare the same module path.\n\nA module is identified by its path, not by the file that contains it, so\ntwo files with the same `module` line cannot both be imported: any `use`\nthat names the path is ambiguous between them. The fix is to give one of\nthe files a different path.\n\nA user file whose path matches a shipped module is not a collision. The\nuser file wins, because the file a person can read is the file they can\nchange.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED4001",
        name: "TYPE_MISMATCH",
        text: "A value of one type used where another was required.",
        example: Some("module a\n\nfn sneaky(time: Clock) -> ()\n  uses Io.write,\n{\n  Io.write(time, \"hello\")\n}\n"),
        example_source: Some("crates/deed-driver/tests/capabilities.rs"),
    },
    Page {
        code: "DEED4002",
        name: "MISSING_FIELDS",
        text: "A record or variant literal that leaves fields out.",
        example: Some("module a\n\nrecord R { a: Int, b: Int, c: Int }\n\nfn f() -> R { R { a: 1 } }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4003",
        name: "UNKNOWN_FIELD",
        text: "A record or variant literal that names a field the type does not have.",
        example: Some("module a\n\nrecord R { a: Int }\n\nfn f() -> R { R { a: 1, z: 2 } }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4004",
        name: "NO_SUCH_FIELD",
        text: "Field access on a type with no such field.",
        example: Some("module a\n\nfn main(sys: System) -> Int\n  uses Io.write,\n{\n  Io.write(sys.network, \"hello\")\n  0\n}\n"),
        example_source: Some("crates/deed-driver/tests/capabilities.rs"),
    },
    Page {
        code: "DEED4005",
        name: "NOT_A_CONSTRUCTOR",
        text: "A literal whose path is not a record or a variant.",
        example: Some("module a\n\nfn g() -> Int { 0 }\n\nfn f() -> Int {\n    let a = g { x: 1 }\n    0\n}\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4006",
        name: "NON_EXHAUSTIVE_MATCH",
        text: "A `match` that does not cover every variant.",
        example: Some("module a\n\nchoice E { A, B, C }\n\nfn f(e: E) -> Int {\n  match e {\n    A => 1,\n  }\n}\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4007",
        name: "WRONG_ARITY",
        text: "A call with the wrong number of arguments.",
        example: Some("module a\n\nfn f(items: List<Int>) -> Int { length(items, 1) }\n"),
        example_source: Some("crates/deed-driver/tests/lists.rs"),
    },
    Page {
        code: "DEED4008",
        name: "UNPROVEN_REFINEMENT",
        text: "A refinement that could not be discharged.\n\nNot a rejection of the program so much as an admission of what the compiler\ncan currently prove. See the tier table in `design/02-syntax.md`.",
        example: Some("module a\n\ntype Positive = Int where value > 0\n\nfn take(n: Positive) -> Int { 0 }\n\nfn f(n: Int) -> Int { take(n) }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4009",
        name: "VIOLATED_REFINEMENT",
        text: "A refinement the compiler proved false.",
        example: Some("module a\n\ntype Positive = Int where value > 0\n\nfn take(n: Positive) -> Int { 0 }\n\nfn f() -> Int { take(0) }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4010",
        name: "TYPE_ALIAS_CYCLE",
        text: "A type alias that expands to itself.",
        example: Some("module a\n\ntype Loop = Loop\n\nfn f(x: Loop) -> Int { 0 }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4011",
        name: "NOT_A_TYPE",
        text: "A name used in type position that does not name a type.",
        example: Some("module a\n\nfn thing() -> Int { 0 }\n\nfn f(x: thing) -> Int { 0 }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4012",
        name: "NOT_CALLABLE",
        text: "A call on something that is not a function.",
        example: Some("module a\n\nfn f(n: Int) -> Int { n(1) }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4013",
        name: "NOT_GENERIC",
        text: "Type arguments applied to a type that takes none.",
        example: Some("module a\n\nrecord Entry<K, V> { key: K, value: V }\n\ntype Table<K, V> = List<Entry<K, V>>\n\nfn f(t: Table<String>) -> Int { length(t) }\n"),
        example_source: Some("crates/deed-driver/tests/generic_types.rs"),
    },
    Page {
        code: "DEED4014",
        name: "CATCH_ALL_ON_CHOICE",
        text: "A `match` on a choice with an arm that matches everything.\n\nExhaustiveness is only worth something if adding a variant breaks the code\nthat has to care. A wildcard arm makes that stop happening quietly.",
        example: Some("module a\n\nchoice E { A, B, C }\n\nfn f(e: E) -> Int {\n  match e {\n    A => 1,\n    _ => 2,\n  }\n}\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4015",
        name: "NOT_ASSIGNABLE",
        text: "An assignment to something that is not handler state.\n\nState is the only mutable thing in Deed. Everything else is a name for a\nvalue, and a name whose value changes partway through a function is the\nsame problem that made shadowing an error.",
        example: Some("module a\n\nfn f(n: Int) -> Int {\n  n = 1\n  n\n}\n"),
        example_source: Some("crates/deed-driver/tests/assignment.rs"),
    },
    Page {
        code: "DEED4016",
        name: "NOT_A_RESULT",
        text: "`?` applied to something that is not a `Result`.",
        example: Some("module a\n\nfn f() -> Result<Int, Int> {\n  let n = 1?\n  ok(n)\n}\n"),
        example_source: Some("crates/deed-driver/tests/result.rs"),
    },
    Page {
        code: "DEED4017",
        name: "TRY_NEEDS_RESULT_RETURN",
        text: "`?` inside a function that does not return a `Result`.",
        example: Some("fn inner() -> Result<Int, Failure> { ok(1) }\n\nfn outer() -> Int {\n  inner()?\n}\n"),
        example_source: Some("crates/deed-driver/tests/result.rs"),
    },
    Page {
        code: "DEED4018",
        name: "PATTERN_MISMATCH",
        text: "A pattern that cannot match the thing it is applied to.",
        example: Some("module a\n\nfn f(n: Int) -> Int {\n  match n {\n    ok(x) => x,\n    err(e) => 0,\n  }\n}\n"),
        example_source: Some("crates/deed-driver/tests/result_patterns.rs"),
    },
    Page {
        code: "DEED4019",
        name: "NOT_A_VALUE",
        text: "A name used in expression position that names a type, not a value.\n\nThis matters more than it looks. Without it a type name in expression\nposition has no type, and a type-less expression is compatible with\neverything, so `Io.write(Console, \"hi\")` would type check and a program\ncould conjure authority by naming it.",
        example: Some("module a\n\nfn sneaky() -> ()\n  uses Io.write,\n{\n  Io.write(Console, \"hello\")\n}\n\nfn main(sys: System) -> Int { 0 }\n"),
        example_source: Some("crates/deed-cli/tests/cli.rs"),
    },
    Page {
        code: "DEED4020",
        name: "NOT_ORDERED",
        text: "`<`, `<=`, `>` or `>=` on a type that has no order.\n\nOrdering used to be accepted for anything, as long as both sides agreed, so\ncomparing two records passed the type checker and failed at runtime with a\nmessage about the interpreter being incomplete. It was not incomplete: there\nis nothing to implement, because there is nothing the comparison could mean.",
        example: Some("module a\n\nrecord Point { x: Int }\n\nfn f(a: Point, b: Point) -> Bool { a < b }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4021",
        name: "OPERATION_MISMATCH",
        text: "A handler operation that does not line up with the effect it implements.\n\nA handler operation writes no types, because the effect already declared\nthem. That only works if the effect is actually consulted, and it was not:\nevery parameter in every handler body was the unknown type, which agrees\nwith everything, so a handler was the least checked code in the language\nwhile being the piece that holds the state and talks to the outside world.",
        example: Some("module a\n\nuse other.{Ledger}\n\nhandler InMemory implements Ledger {\n  state total: Int\n\n  fn post(amount) -> () {\n    total = total + amount\n  }\n\n  fn invent() -> () {}\n}\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4022",
        name: "NOT_A_LIST",
        text: "A list operation applied to something that is not a list.",
        example: Some("module a\n\nfn f(text: String) -> Int {\n  for c in text with n = 0 {\n    n + 1\n  }\n}\n"),
        example_source: Some("crates/deed-driver/tests/iteration.rs"),
    },
    Page {
        code: "DEED4023",
        name: "UNDETERMINED_TYPE_PARAM",
        text: "A type parameter that appears in no parameter's type.\n\nEvery call has to be able to work out what a type parameter is from the\narguments alone, and the only place it can look is the parameter types.\nThe alternative is writing type arguments at the call site, which needs\n`first<String>([])` to parse, which is the `f<a>(b)` versus `f < a > (b)`\nambiguity, and P2 has a budget for exactly this kind of thing.\n\nIt is also the same claim the rest of the language makes: a signature is\ncomplete, and one with a hole a caller has to fill from somewhere else is\nnot.\n\nTwo things in the language break it and are allowed to, because they are\nbuilt in rather than declared. `ok(x)` is a `Result<T, unknown>` and\n`err(e)` is a `Result<unknown, E>`, so each mentions a parameter no\nargument decides and answers it with the unknown type, which absorbs. That\nis the shortcut holding `Result` in the language: a declared `choice` can\nalready be built and matched with named fields, and what nobody can write\nis the constructor. See the open question in `design/02-syntax.md`, where\nletting the rule take that answer generally is one of the three ways out.",
        example: Some("module a\n\nchoice Outcome<T, E> {\n    Good { value: T },\n    Bad { error: E },\n}\n\nfn good<T, E>(value: T) -> Outcome<T, E> {\n    Good { value: value }\n}\n"),
        example_source: Some("crates/deed-driver/tests/generic_types.rs"),
    },
    Page {
        code: "DEED4024",
        name: "GENERIC_AS_VALUE",
        text: "A generic function used as a value rather than called.\n\nOne expression has one type here, and a generic function named rather than\ncalled has as many as there are ways to call it. What would make this work\nis a polymorphic value, which is a much larger thing than substituting into\na signature at a call site.",
        example: Some("module a\n\nfn first<T>(items: List<T>) -> Result<T, String> { at(items, 0) }\n\nfn apply(f: Fn(List<Int>) -> Result<Int, String>) -> Int { 0 }\n\nfn f() -> Int { apply(first) }\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4025",
        name: "BROKEN_PRECONDITION",
        text: "A call the checker can see does not satisfy what the callee requires.\n\nA precondition is the caller's job, so it is reported where the call was\nwritten rather than inside the function that stated it. Only when the facts\nat the call site settle the clause the wrong way; not knowing is the\nordinary case and leaves the runtime check to answer.",
        example: Some("fn nth(items: List<Int>, index: Int) -> Result<Int, String>\n  where\n    index >= 0,\n    index < length(items),\n{\n  at(items, index)\n}\n\nfn fourth() -> Result<Int, String> { nth([1, 2, 3], 3) }\n"),
        example_source: Some("crates/deed-driver/tests/proving.rs"),
    },
    Page {
        code: "DEED4026",
        name: "DISCARDED_VALUE",
        text: "A statement whose value nobody reads.\n\nA block's value is its tail, so any other expression in it is there for\nwhat it does. One that produces a value has nowhere to put it, and the two\nways to get here are a result that was meant to be looked at and a line\nthat was meant to continue the one above it. The second became possible to\ntell apart when an expression started ending at the end of a line.",
        example: Some("module a\n\nfn twice(n: Int) -> Int { n + n }\n\nfn f(n: Int) -> Int {\n  twice(n)\n  n\n}\n"),
        example_source: Some("crates/deed-typeck/tests/checking.rs"),
    },
    Page {
        code: "DEED4027",
        name: "WHILE_WITHOUT_ACCUMULATOR",
        text: "A `for ... while ...` with nothing to be about.\n\nThe condition is read before each turn and sees the accumulator, so a walk\nwithout one gives it nothing that changes. Such a condition either stops\nthe walk before the first turn or never stops it, and neither is a thing\nanybody meant to write.",
        example: Some("module a\n\nfn f(items: List<Int>) -> () {\n  for item in items while true {\n    ()\n  }\n}\n"),
        example_source: Some("crates/deed-driver/tests/iteration.rs"),
    },
    Page {
        code: "DEED4028",
        name: "REFINEMENT_TYPE_PARAM",
        text: "Type parameters on an alias that also carries a predicate.\n\nAn alias with no predicate is a name for a type and expands to it, so\nparameters on one are the substitution a `record` already does.\nA refinement is not that: it is a nominal type whose predicate is checked,\nand a predicate about a value whose type is not decided yet has nothing it\ncan say. `length(value) > 0` over an unknown `T` is not a question with an\nanswer, so the parameters are refused rather than accepted and ignored.",
        example: Some("module a\n\ntype Positive<T> = Int where value > 0\n"),
        example_source: Some("crates/deed-driver/tests/generic_types.rs"),
    },
    Page {
        code: "DEED4029",
        name: "HANDLER_MISSING_OPERATION",
        text: "A handler that leaves out an operation the effect it implements declares.\n\n`implements` is a claim, and the check for it only ran one way: an\noperation the effect does not declare was DEED4021, and an operation the\neffect declares and the handler leaves out was nothing at all until the\nmissing one was reached at run time.\n\nHalf a handler is not a smaller handler. A `with` block discharges the\neffect rather than the operations named inside it, so installing one is a\nclaim that every call underneath has somewhere to go, and a row saying\n`uses Counter.total` is a caller taking that claim at its word.",
        example: None,
        example_source: None,
    },
    Page {
        code: "DEED4030",
        name: "CLOSURE_OVER_STATE",
        text: "A closure that names the handler state around it.\n\nA closure captures the frame by value, so everything else it sees is a\nnumber it was handed. Handler state would be the one thing it saw through a\nreference, and the reference is to something whose lifetime is a `with`\nblock while the closure's is not. The interpreter used to read it out of\nwhichever handler happened to be innermost when the call landed, so a\nclosure written in one handler and called under another quietly answered\nout of the other one's table when the two shared a state name.\n\nCapturing the handler instead was the other way out, and it is refused for\na reason that outlives the interpreter: the closure's type would not say it.\n`Fn() -> Int` claims to take nothing and perform nothing, and a value that\nis also a live window onto a particular handler's state carries an input\nand a lifetime through a signature that mentions neither. A signature is\ncomplete here, which is the same rule `DEED4023` and the row before the\narrow are both instances of.\n\nWhat to write instead is the snapshot the rest of the language already\ntakes: read the state into a local and let the closure capture that. The\nvalue is then a number, which is what a `Fn() -> Int` says it is.",
        example: Some("module a\n\neffect Give {\n    fn bump() -> Fn() -> ()\n}\n\nhandler A implements Give {\n    state n: Int\n\n    fn bump() -> Fn() -> () {\n        || { n = 1 }\n    }\n}\n"),
        example_source: Some("crates/deed-driver/tests/handler_state.rs"),
    },
    Page {
        code: "DEED4031",
        name: "OPERATOR_SHAPE",
        text: "`operator + = added` where `added` is not something an operator can mean.\n\nA binding says an operator, written between two values of one type, means a\nfunction. The shape that makes that true is narrow and every part of it is\nload-bearing:\n\n- Two parameters and a result, all of one type, so `a + b + c` is written\n  the way it reads and the operator hands back what it was given.\n- That type declared in this module, so the meaning of `+` on a type is\n  decided in one file. The same reasoning keeps module resolution free of a\n  search path.\n- Nothing generic, because the operand types are what choose the function\n  and a type parameter is not a type yet. That is the trait question, and\n  it is not this one.\n- An empty row. An operator is reachable from a contract clause, and a\n  clause that performs something is a clause that cannot be read as a\n  question about values.\n\nOne code and several sentences, because they are one mistake: the function\nnamed is not the shape an operator is. See\n`design/decisions/2026-08-03-operators-bound-to-functions.md`.",
        example: Some("module probe\n\nrecord Money {\n    cents: Int,\n}\n\neffect Log {\n    fn note(line: String) -> ()\n}\n\noperator + = loud\n\nfn loud(left: Money, right: Money) -> Money\n  uses\n    Log.note,\n{\n    Log.note(\"adding\")\n    Money { cents: left.cents + right.cents }\n}\n"),
        example_source: Some("crates/deed-driver/tests/operators.rs"),
    },
    Page {
        code: "DEED4032",
        name: "NOT_HASHABLE",
        text: "`hash` given something with no parts to read.\n\nA hash here is the equality walk with a different accumulator, so what it\ntakes is what `==` takes, minus the two things equality answers by\nidentity rather than by content.\n\nA function value is one: two closures are equal when they are the same\nclosure rather than when they hold the same thing, so the only thing to\nhash would be an address, and an address is not stable across runs or\nacross engines. A capability is the other, and it holds nothing a program\nis allowed to look at, which is what makes being handed one mean anything.\n\nRefused here rather than by the backend, because nothing is going to make\neither work later. See\n`design/decisions/2026-08-05-a-hash-is-the-equality-walk.md`.",
        example: Some("module a\n\nfn twice(n: Int) -> Int { n + n }\n\nfn answer() -> Int {\n    let f = twice\n    hash(f)\n}\n"),
        example_source: Some("crates/deed-driver/tests/hashing.rs"),
    },
];
