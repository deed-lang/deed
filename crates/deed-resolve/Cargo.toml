[package]
name = "deed-resolve"
description = "Binds every name in a Deed module to a declaration"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
deed-ast = { path = "../deed-ast" }
deed-diagnostics = { path = "../deed-diagnostics" }

[dev-dependencies]
deed-lexer = { path = "../deed-lexer" }
deed-parser = { path = "../deed-parser" }

[lints]
workspace = true
