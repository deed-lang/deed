[package]
name = "deed-wasm"
description = "The boundary a page calls across: linear memory in, JSON out, no binding generator"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
deed-ast = { path = "../deed-ast" }
deed-diagnostics = { path = "../deed-diagnostics" }
deed-driver = { path = "../deed-driver" }
deed-explain = { path = "../deed-explain" }
deed-fmt = { path = "../deed-fmt" }
deed-interp = { path = "../deed-interp" }
deed-lexer = { path = "../deed-lexer" }

[lints]
workspace = true
