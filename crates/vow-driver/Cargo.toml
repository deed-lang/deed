[package]
name = "vow-driver"
description = "Runs every Vow compiler pass over a file"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
vow-ast = { path = "../vow-ast" }
vow-diagnostics = { path = "../vow-diagnostics" }
vow-effects = { path = "../vow-effects" }
vow-lexer = { path = "../vow-lexer" }
vow-parser = { path = "../vow-parser" }
vow-resolve = { path = "../vow-resolve" }
vow-typeck = { path = "../vow-typeck" }

[dev-dependencies]
vow-interp = { path = "../vow-interp" }

[lints]
workspace = true
