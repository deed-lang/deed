[package]
name = "vow-parser"
description = "Builds a Vow syntax tree from tokens"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
vow-ast = { path = "../vow-ast" }
vow-diagnostics = { path = "../vow-diagnostics" }
vow-lexer = { path = "../vow-lexer" }

[lints]
workspace = true
