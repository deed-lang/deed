[package]
name = "vow-resolve"
description = "Binds every name in a Vow module to a declaration"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
vow-ast = { path = "../vow-ast" }
vow-diagnostics = { path = "../vow-diagnostics" }

[dev-dependencies]
vow-lexer = { path = "../vow-lexer" }
vow-parser = { path = "../vow-parser" }

[lints]
workspace = true
